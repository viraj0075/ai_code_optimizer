{
  "name": "github-cost-optimizer",
  "version": "1.0.0",
  "description": "GitHub Action that scans Terraform, YAML, JSON and application code for cloud cost optimization opportunities.",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "analyze": "node src/index.js --local",
    "package": "ncc build src/index.js --license licenses.txt -o dist && node -e \"import fs from 'node:fs'; fs.renameSync('dist/index.js', 'dist/index.cjs')\""
  },
  "keywords": [
    "github-actions",
    "cost-optimization",
    "aws",
    "terraform",
    "devops"
  ],
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.1",
    "dotenv": "^17.4.2",
    "js-yaml": "^5.4.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3"
  }
}